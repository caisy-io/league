import styled from "styled-components";

const SDatePickerTimePickerWrapper = styled.div`
  height: 44px;
  background: var(--ui-01);
  border: 1px solid var(--ui-03);
  box-sizing: border-box;
  border-radius: 8px;
  display: flex;
  align-items: center;
  padding: 0 8px;
  
  svg {
    width: 20px;
    height: 20px;
    color: var(--icon-03);
    margin-right: 8px;
  }
`;

export default SDatePickerTimePickerWrapper;
