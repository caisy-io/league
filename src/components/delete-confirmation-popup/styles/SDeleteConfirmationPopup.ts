import styled from "styled-components";

export const SDeleteConfirmationPopup = styled.div`
  width: 312px;
  border-radius: 8px;
  box-shadow: var(--box-shadow-light-pop-elevation);
  background-color: var(--ui-01);
`;
