import styled from "styled-components";

export const SUsageAmountItem = styled.div`
  background-color: var(--ui-01);
  padding: 24px;
  border-radius: 6px;
  border: 1px solid var(--ui-03);

  display: flex;
  flex-direction: column;
  gap: 12px;
`;
