import styled from "styled-components";

export const SUsageAmountItemUsageGraphContainer = styled.div`
  display: flex;
  gap: 12px;
  align-items: center;

  font-weight: 600;
  font-size: 40px;
  line-height: 48px;
  letter-spacing: -0.02em;

  color: var(--text-01);
`;
