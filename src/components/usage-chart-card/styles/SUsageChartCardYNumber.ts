import styled from "styled-components";
import { CSSProgressiveCaption02Medium } from "../../../constants/styles";

export const SUsageChartCardYNumber = styled.div`
  ${CSSProgressiveCaption02Medium};
  color: var(--text-03);
  transform: translateY(-7px);
`;
