import styled from "styled-components";

export const SUsageChartCard = styled.div`
  display: flex;
  background-color: var(--ui-01);
  padding: 24px 24px 0 24px;
  border-radius: 6px;
  gap: 24px;
  border: 1px solid var(--ui-03);
  height: 400px;
  box-sizing: border-box;
`;
